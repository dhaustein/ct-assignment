[project]
name = "api-sdk"
version = "0.1.0"
description = "API SDK in Python generated by OpenAPI Generator"
readme = "README.md"
authors = [
    { name = "dhaustein" }
]
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["uv_build>=0.8.8,<0.9.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "openapi-generator-cli>=7.15.0",
]
